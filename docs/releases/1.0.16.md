# v1.0.16 — Agente independente do app + restart script

## fix
- `stacker-agent` não depende mais do `app` — sobe mesmo com site fora (updates remotos continuam possíveis)
- `docker/restart-stacker-agent.sh` — sobe/recria só o agente na VPS

## stacker-hub (deploy separado)
- Jobs `queued` há 15+ min viram `failed` (cron a cada 5 min)
- Portal: aviso quando agente offline + update na fila; botão cancelar fila
