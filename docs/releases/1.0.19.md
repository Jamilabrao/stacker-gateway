# v1.0.19 — Fix build context no apply remoto

## fix
- Apply dentro do stacker-agent usa `/gateway` como project-directory (não `/opt/getfy`, que não existe no container)
- `docker build` direto em vez de `compose build` — evita "unable to prepare context: path /opt/getfy not found"
