# v1.0.20 — Apply longo sem timeout falso

## fix
- Agente: apply assíncrono (não bloqueia heartbeat) + keepalive a cada 60s durante docker build
- stacker-hub: timeout de apply aumentado para 45 min (fila continua 15 min)
- Portal: aviso quando apply em andamento e agente parece offline
