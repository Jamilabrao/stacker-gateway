## Update remoto estável e changelog no portal

### Correções
- [fix] Agente não reinicia durante o apply (compose up exclui stacker-agent)
- [fix] Compose project detecta `getfy` em vez de `gateway` no mount `/gateway`
- [fix] `docker/php/uploads.ini` recriado quando vira diretório por mount Docker

### Melhorias
- [improvement] Jobs de update presos são reconciliados após timeout
- [feature] Changelog versionado em `docs/releases/{VERSION}.md` publicado pelo CI
