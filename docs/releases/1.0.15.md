# v1.0.15 — Fix .env ausente no apply remoto

## fix
- Cria `/opt/getfy/.env` a partir de `.docker/stack.env` antes do `docker compose` (exigido pelo `env_file` do stacker-agent com `--project-directory`)
- Preserva variáveis `STACKER_*` do agente em execução ao gerar o `.env`
- Portal: botão "Tentar aplicar novamente" quando o apply falhou mas a versão já foi copiada
