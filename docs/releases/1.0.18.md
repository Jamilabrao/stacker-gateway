# v1.0.18 — Fix compose env_file .env no apply remoto

## fix
- Remove `env_file: .env` do stacker-agent — compose falhava ao validar `/opt/getfy/.env` durante `build app` (apply roda dentro do agente)
- STACKER_* via `environment:` + `--env-file .env` no compose (opcional no apply, obrigatório ao subir agente)
- `stacker-apply-update.sh` passa `--env-file .env` quando existir em `/gateway`
