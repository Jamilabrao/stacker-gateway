# v1.0.17 — Fix apply dentro do stacker-agent

## fix
- `ensure-host-dotenv.sh` usa `/gateway` (mount), não `/opt/getfy` — path do host não existe dentro do container
- `stacker-apply-update.sh` chama ensure-host-dotenv sem passar GETFY_HOST_DIR
