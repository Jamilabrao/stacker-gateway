# v1.0.14 — Fix apply remoto (uploads.ini / compose paths)

## fix
- Remove bind mount de `docker/php/uploads.ini` — limites PHP ficam só na imagem (Dockerfile), evitando erro "not a directory" quando o compose roda dentro do stacker-agent
- `stacker-apply-update.sh` usa `--project-directory` com `GETFY_HOST_DIR` (caminho real no host, ex. `/opt/getfy`)
- `recover-stack.sh` corrige `uploads.ini` e rebuild do app antes do `up`
